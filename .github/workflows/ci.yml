name: CI/CD Pipeline — Anonymous Buyer Feedback

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]

jobs:
  test-and-build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Setup Node.js v22
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Install Dependencies
        run: npm install

      - name: Verify Compact Contract Source
        run: |
          echo "=== Compact Contract Source (anonymous_buyer_feedback.compact) ==="
          cat contracts/anonymous_buyer_feedback.compact
          echo ""
          echo "=== Compact Contract Source (counter.compact) ==="
          cat contracts/counter.compact
          echo ""
          echo "✅ Compact contract source verified."
          echo "📦 Managed output verified in managed/contract/"
          ls -la managed/contract/

      - name: Verify Managed Contract Output
        run: |
          echo "=== Managed Contract Type Definitions ==="
          cat managed/contract/index.d.ts
          echo ""
          echo "✅ Managed contract artifacts present (index.js + index.d.ts)"

      - name: Run Vitest Unit Tests
        run: npm test

      - name: Build Next.js Web Platform
        run: npm run build
